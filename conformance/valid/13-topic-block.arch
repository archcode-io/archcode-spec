# conformance: valid — §5.3 a topic with via, publisher and subscriber
# expect: objects=4 relations=0 placements=0
# expect: diagnostics=none
broker kafka "Kafka" { transit }
service checkout "Checkout"
service billing "Billing"
topic order.placed {
  via kafka
  publisher checkout
  subscriber billing
}
