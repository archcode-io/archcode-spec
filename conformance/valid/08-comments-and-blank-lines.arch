# conformance: valid — §3 comments anywhere, blank lines kept
# expect: objects=2 relations=1 placements=0
# expect: diagnostics=none

# a comment between statements
service a "A"   # trailing comment


service b "B"
  # indented comment
a calls b   # and after a relation
